import type { StoryObj, Meta } from '@storybook/react'
import { Button, Teste } from '@ignite-ui/react'

export const Primary: StoryObj = {}

export default {
  title: 'Button',
  component: Teste,
} as Meta
